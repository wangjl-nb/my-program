let y:int;
fn fib(x: int) -> int {
    if x<=1 {
        if y<=2{
            return 1;
        }
    }
    let result: int = fib(x - 1);
    result = result + fib(x - 2);
    return result;
}

fn fib1(x: int) -> int {
    return;
}

fn main() -> int {
    let i: int = 0;
    let j: int;
    j = getint();
    while i < j {
        putint(i);
        putchar(32);
        putint(fib(i));
        putln();
        i = i + 1;
    }
    return 0;
}
